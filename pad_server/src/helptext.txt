pad 0.0.0
2024 CU InSpace

DESCRIPTION:
    Emulates the pad control box.

USAGE:
    pad [options]

OPTIONS:
    -t file     A CSV file containing sensor data telemetry to transmit. If not
                specified, no sensor data telemetry will be sent.

EXAMPLES:
    pad -t ../thecoldhasflown.csv
